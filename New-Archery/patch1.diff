*** Begin Patch
*** Update File: archerysec/user_management/views.py
@@
     def get(self, request):
         org = Organization.objects.all()
         if str(getattr(request.user, "role", "")) == "IT User":
             roles = UserRoles.objects.filter(role__in=["User", "Analyst", "IT User"])  # restrict
         else:
             roles = UserRoles.objects.exclude(role__in=["Viewer"]) 
-
-        return Response({"org": org, "roles": roles})
+        # Support PRG: repopulate values/errors saved in session
+        form_state = request.session.pop("add_user_form", None)
+        ctx = {"org": org, "roles": roles}
+        if form_state:
+            ctx.update(form_state)
+        return Response(ctx)
*** End Patch
